//import "/js/darkreader/darkreader.js";
// see https://github.com/darkreader/darkreader
// darkreader can be used for websites
// all commented lines are from the example, but only the enable=true is necessary
//DarkReader.enable({ brightness: 50, contrast: 55,    sepia: 0 });

// this was slow and had the white page blink up before it became dark, so I used the function below to export the css and it is now saved in
// https://www.jugglingpatterns.de/mediawiki/index.php/MediaWiki:Common.css
// the css is loaded via: "/js/darkreader_export.css";
//DarkReader.enable(true);

//DarkReader.disable();
// Enable when the system color scheme is dark.
//DarkReader.auto({    brightness: 50,    contrast: 55,    sepia: 0 });
// Stop watching for the system color scheme.
//DarkReader.auto(true);
// Get the generated CSS of Dark Reader returned as a string.
//const CSS = await DarkReader.exportGeneratedCSS();
//console.log(CSS);
// Check if Dark Reader is enabled.
//const isEnabled = DarkReader.isEnabled();

